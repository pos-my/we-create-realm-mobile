import Realm from "realm";

//const app = new Realm.App({id: "<your Realm app ID here>"}); // Set Realm app ID here.
const app = new Realm.App({id: "pokeapp-cxyct"}); // Set Realm app ID here.
export default app;

